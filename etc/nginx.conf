user www-data;
worker_processes auto;
pid /run/nginx.pid;

http {

	server {
	
	listen 80 default;

	location ^~ /uploads/ {
		root /home/box/web;
	}

	location ~* \.(gif|jpe?g|png)$ {
		root /home/box/web/public;	
	}

}
